/**
 * Dashboard components barrel export.
 *
 * Provides centralized imports for all dashboard variants.
 * Each dashboard is role-specific and contains unique UI elements.
 *
 * @usage
 * import { InternDashboard, EmployeeDashboard } from '@/components/dashboards';
 */

export { default as InternDashboard } from './InternDashboard';
export { default as EmployeeDashboard } from './EmployeeDashboard';

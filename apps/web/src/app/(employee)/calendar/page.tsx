import { CompanyCalendarView } from '@/components/CompanyCalendarView';

export default function CompanyCalendarPage() {
  return (
    <CompanyCalendarView
      title="Company Calendar"
      description="View the shared company schedule sourced from Google Calendar. Open any event to inspect timing and add it to your own calendar."
    />
  );
}
-- NOTE: INTENTIONALLY DISABLED / NO-OP
-- ---------------------------------------------------------------------------
-- This migration was originally added as a policy override safety patch.
-- It is now redundant because base marketing migrations were already aligned.
--
-- Keep this file (do not delete) so migration history remains traceable.
-- No schema or policy changes are executed by this migration.
-- ---------------------------------------------------------------------------

BEGIN;
-- no-op on purpose
COMMIT;

